[안내]

- Everything처럼 모든 정보를 즉시 실시간으로 갱신하는 방식은 NAS 이용 환경에서 잦은 접근 부담이 생길 수 있어, 충분히 테스트하며 단계적으로 적용할 예정입니다. 현재는 파일 추가/수정/삭제가 감지되면 잠시 후 변경분만 갱신할 수 있도록 알려주고, 스캔 버튼 클릭 시 변경된 부분만 우선 스캔하도록 개선해 두었습니다.
- v9.0.0에서는 저장 구조와 결과 목록 표시, 개별 분석, 통합 비교, 목록 검사 전반을 크게 최적화해 대량 라이브러리에서 시작 로딩과 목록 표시, 분석 후 대기 시간이 줄어들도록 개선했습니다. 기존 스캔 캐시와 라이브러리 목록은 새 SQLite 형식으로 자동 변환되며, 변환이 정상 완료되면 기존 JSON은 .bak으로 한 번 보관한 뒤 다음 실행에서 정리됩니다. 다만 캐시나 목록 변환이 반복 실패하는 경우에는 안전을 위해 빈 캐시/목록으로 시작할 수 있으므로, 이때는 폴더 스캔이나 필요한 분석을 다시 실행해 주세요.

## v9.0.0

- 대량 결과 목록을 기본 500개씩 페이지 단위로 표시해, 많은 시리즈를 한 번에 그릴 때의 대기 시간을 줄였습니다.
- 검색어가 없는 일반 목록 표시에서는 현재 페이지에 필요한 행만 생성하도록 개선해, 첫 페이지 표시 중 전체 목록을 모두 화면용 행으로 변환하던 대기 시간을 줄였습니다.
- 환경 설정의 화면 탭에서 결과 목록 페이지 크기를 100개, 200개, 500개, 1000개, 전체 중에서 선택할 수 있도록 추가했습니다.
- 이전 실행 캐시에 전체 표시 값이 남아 있어도 설정에 페이지 크기 값이 없으면 기본값인 500개 표시로 시작하도록 보강했습니다.
- 결과 목록 검색, 필터, 정렬은 전체 결과 기준으로 먼저 적용한 뒤 현재 페이지를 표시하도록 해 페이지 이동 중에도 정렬과 검색 결과가 자연스럽게 유지되도록 했습니다.
- 중요 안내가 최근 5개 릴리즈 목록에서 밀려도 조건에 맞는 버전에서는 계속 표시되도록 보강했습니다.
- 스캔 버튼 옆 메뉴에서 전체 스캔과 지금 변경 확인을 바로 선택할 수 있도록 하고, 파일 변경이 감지되면 추가/수정/삭제 개수를 함께 표시하도록 개선했습니다.
- 개별 분석과 통합 비교 결과 표시가 한 번에 몰려 UI가 끊기지 않도록 결과 표시를 작은 단위로 나누고, 통합 비교는 분석 중 완료된 항목부터 먼저 보여주도록 개선했습니다.
- 개별 분석도 여러 파일/권을 병렬로 분석하되 결과는 원래 권 순서대로 표시해, 순서가 섞이지 않으면서 느린 파일 하나 때문에 전체 분석이 늦어지는 부담을 줄였습니다.
- 개별 분석/통합 비교 창을 닫을 때 남은 결과 표시 큐를 먼저 정리하고 창 숨김을 우선 처리해, 스캔 완료 직후 닫기 반응이 늦어지는 현상을 줄였습니다.
- 개별 분석이 끝났는데도 창 상단 문구가 계속 분석 중으로 남던 문제를 수정했습니다.
- 개별 분석/통합 비교 완료 직후 대용량 캐시 저장이 바로 실행되어 목록 스크롤까지 버벅일 수 있던 후처리 부담을 줄이도록, 캐시 저장을 더 늦추고 캐시 파일 저장 크기를 줄였습니다.
- 개별 분석/통합 비교 후 지연 저장되는 스캔 캐시가 전체 재작성으로 이어질 수 있던 경로를 변경 항목 저장으로 보강해, 완료 몇 초 뒤 UI가 끊기는 현상을 줄였습니다.
- 변경분 갱신은 속도를 위해 이미 감지된 변경 범위만 처리하되, 추가/수정/삭제 대상 파일의 현재 존재 여부에 맞춰 누락과 오삭제가 생기지 않도록 보강했습니다.
- 상위 스캔 경로와 그 하위 경로가 함께 체크되어 있어도 같은 파일을 중복 수집하지 않도록 스캔 시작 전 경로를 정리합니다.
- 파일 변경 확인 중 스캔을 시작하면 진행 중이던 변경 확인 결과가 뒤늦게 도착해도 현재 스캔 상태를 덮어쓰지 않도록 보강했습니다.
- 지금 변경 확인에서 변경된 파일이 없을 때 상태줄에 변경 없음 완료 문구가 표시되도록 보완했습니다.
- 상위 스캔 경로는 체크하고 하위 경로는 체크 해제한 경우, 체크 해제한 하위 경로는 스캔/변경 확인에서 제외되도록 보강했습니다.
- 스캔 캐시 저장 방식을 SQLite 기반으로 전환해 개별 분석/통합 비교/스캔 뒤 대용량 캐시 저장 때문에 UI가 멈추는 시간을 줄이도록 개선했습니다.
- 기존 스캔 캐시가 있으면 시작 시 새 형식으로 변환하고, 변환이 반복 실패하면 빈 캐시로 시작해 새 스캔을 권장하도록 안내를 추가했습니다.
- SQLite 캐시 변환이 끝난 뒤 기존 JSON 캐시는 바로 삭제하지 않고 `.bak`으로 한 번 보관한 다음, 다음 실행에서 SQLite 캐시가 확인되면 자동 정리하도록 보강했습니다.
- SQLite 캐시 파일이 손상된 상태에서는 기존 `.bak` 백업을 지우지 않도록 복구 안정성을 보강했습니다.
- 캐시를 모두 비운 뒤 새로 스캔/분석하는 경우에도 스캔 캐시와 라이브러리 목록이 다시 JSON으로 저장되지 않고 SQLite 저장소로 시작되도록 보강했습니다.
- 라이브러리 목록 저장 방식을 SQLite 기반으로 전환해 시작 로딩과 스캔 완료 후 목록 저장 대기 시간을 줄이도록 개선했습니다.
- 기존 라이브러리 목록 JSON은 새 형식 변환 후 `.bak`으로 한 번 보관하고, 다음 실행에서 SQLite 목록이 확인되면 자동 정리하도록 보강했습니다.
- SQLite 라이브러리 목록 파일이 손상된 상태에서는 기존 `.bak` 백업을 지우지 않도록 복구 안정성을 보강했습니다.
- 변경분 갱신 후 라이브러리 목록을 저장할 때 전체 목록을 다시 쓰지 않고 변경된 시리즈만 반영하도록 개선해, 부분 갱신 완료 후 대기 시간을 줄였습니다.
- 검색/필터가 없는 기본 목록 화면에서는 SQLite에서 현재 페이지에 필요한 시리즈만 우선 읽어 표시하도록 해, 대량 목록의 페이지 표시 부담을 줄였습니다.
- 앱 시작 시 라이브러리 전체 목록을 모두 불러오기 전에 SQLite에서 첫 페이지를 먼저 표시하고, 전체 목록은 이어서 백그라운드로 보강 로드하도록 개선했습니다.
- 체크 해제한 스캔 경로가 있을 때도 SQLite 페이지 목록이 체크된 경로만 기준으로 표시되도록 보강했습니다.
- 경로 체크 상태 변화 등으로 현재 페이지 번호가 범위를 벗어난 경우에도 마지막 유효 페이지로 맞춰 표시하도록 보강했습니다.
- 검색 실행 후 메인창이 앞으로 오면서 검색창 포커스가 다시 잡힐 때 입력한 검색어가 비워질 수 있던 포커스 타이밍을 보강했습니다.
- 목록 검사 성능 병목을 확인할 수 있도록 입력 분해, 분류, 매칭, 점수 계산, 결과 표시 시간 로그를 더 세분화했습니다.
- 목록 검사 후보 매칭 중 prefix/alias/related fallback이 전체 시리즈를 훑는 부담을 줄이도록 기존 후보 토큰 인덱스를 우선 사용하게 개선했습니다.
- 목록 검사 prefix 후보 찾기를 정렬된 정규화 제목 범위 조회로 바꿔, 단일 토큰 입력에서 큰 후보군을 매번 훑는 부담을 줄였습니다.
- 목록 검사 매칭 병목을 더 좁혀볼 수 있도록 런타임 캐시, 쿼리 준비, 정확 매칭, 확신 검사, 표시 제목/보유 권수 계산 시간을 로그에 추가했습니다.
- 목록 검사에서 `1~8 완결 1500`처럼 끝에 붙은 가격/메타 숫자를 요청 권수로 오인해 부분 보유로 표시하던 문제를 수정했습니다.
- 목록 검사 창의 검사 시작 버튼 문구가 깨져 보일 수 있던 문제를 보강했습니다.
- 목록 검사 첫 실행 시 각 시리즈의 모든 파일명을 미리 파싱하던 부담을 줄이고, 필요한 후보만 지연 확인하도록 개선해 첫 검사 대기 시간을 줄였습니다.
- 목록 검사 alias fallback에서 후보가 전혀 좁혀지지 않은 경우 전체 시리즈를 다시 훑지 않도록 해, 일부 입력에서 10초 이상 멈추던 경로를 줄였습니다.
- 결과 목록 상태줄에서 페이지 표시 중에도 전체 목록 용량과 현재 페이지 용량을 구분해 보여주도록 개선했습니다.

## v8.13.3

- 스캔 결과를 목록에 표시할 때 같은 결과를 다시 그리는 작업을 줄여, 대량 목록 표시와 반복 검색/필터 전환의 체감 대기 시간을 줄였습니다.
- 결과 목록 표시 중 진행률이 실제 오래 걸리는 단계 비율에 더 가깝게 올라가도록 조정했습니다.

## v8.13.2

- 새 설치나 자동 업데이트 직후 설정 파일에 목록 컬럼 너비가 아직 없을 때 결과 목록 컬럼이 좁게 초기화되던 문제를 수정했습니다.
- 자동 업데이트 직후 패치노트 표시 기준 버전을 보존해, 첫 표시 타이밍을 놓쳐도 다음 시작에서 패치노트를 볼 수 있도록 보강했습니다.

## v8.13.1

- 자동 업데이트 준비 중 현재 실행에 필요한 임시 런타임 파일이 정리되어 업데이트가 실패할 수 있던 문제를 수정했습니다.

## v8.13.0

- 일부 배포 환경에서 결과 목록 표지 미리보기가 "표지를 불러오지 못했습니다."로만 표시되던 문제를 수정했습니다.
