[안내]

- Everything처럼 모든 정보를 즉시 실시간으로 갱신하는 방식은 NAS 이용 환경에서 잦은 접근 부담이 생길 수 있어, 충분히 테스트하며 단계적으로 적용할 예정입니다. 현재는 파일 추가/수정/삭제가 감지되면 잠시 후 변경분만 갱신할 수 있도록 알려주고, 스캔 버튼 클릭 시 변경된 부분만 우선 스캔하도록 개선해 두었습니다.
- v9.0.0버전 부터는 저장 구조와 결과 목록 표시, 개별 분석, 통합 비교, 목록 검사 전반을 크게 최적화해 대량 라이브러리에서 시작 로딩과 목록 표시, 분석 후 대기 시간이 줄어들도록 개선했습니다. 기존 스캔 캐시와 라이브러리 목록은 새 SQLite 형식으로 자동 변환되며, 변환이 정상 완료되면 기존 JSON은 .bak으로 한 번 보관한 뒤 다음 실행에서 정리됩니다. 다만 캐시나 목록 변환이 반복 실패하는 경우에는 안전을 위해 빈 캐시/목록으로 시작할 수 있으므로, 이때는 폴더 스캔이나 필요한 분석을 다시 실행해 주세요.
- 라이브러리 건강 진단은 이제 누락 의심, 중복 의심, 이름 정리, 품질/출처 경고를 검토하는 단계에서 더 나아가 추천 파일명을 미리 확인하고 실제 이름 변경까지 진행할 수 있도록 확장되었습니다. 이름 변경 전에는 원본 파일 존재 여부, 같은 폴더 이름 충돌, 변경 계획 중복을 먼저 확인하고 안전한 항목만 기본 선택되며, 권/화 단위도 미리보기에서 개별 또는 일괄로 바꿀 수 있습니다. 다만 건강 진단은 라이브러리 정리 보조 도구이므로, 탐지 결과나 추천명이 이상한 경우 파일명/경로와 기대한 결과를 자세히 적어 피드백으로 보내 주세요. 정리 과정에서 불편한 점, 새 규칙 제안, 더 좋은 아이디어도 함께 보내 주시면 이후 개선에 적극 반영하겠습니다.
- 여러 드라이브를 한 번에 연결할 수 없는 환경에서 드라이브를 바꿔 끼우며 하나의 라이브러리 목록으로 누적 관리하는 기능은 현재 지원하지 않습니다. 현재 전체 스캔은 연결된 경로를 기준으로 목록을 갱신하므로, 다른 드라이브를 분리한 상태에서 스캔하면 해당 드라이브의 기존 목록이 사라질 수 있습니다. 오프라인 드라이브의 목록을 보존하면서 연결된 드라이브만 갱신하는 다중 라이브러리 관리 방식은 향후 적용 가능성을 검토하겠습니다. 현재는 드라이브별 프로그램과 데이터베이스를 분리해 사용하는 것이 안전합니다.

## v9.0.62

- 개별 분석의 압축 파일 구조 정리에서 최상위 폴더뿐 아니라 ZIP/CBZ 안에 내부 ZIP/CBZ 하나만 들어 있는 경우에도 내용물을 한 단계 위로 올려 정리할 수 있도록 개선했습니다.

## v9.0.61

- 건강 진단에서 제목과 같은 폴더 안에 `시리즈명 01.zip`, `시리즈명 02.zip`처럼 권 표기 없이 번호만 붙은 파일이 여러 개 있으면 `01권`, `02권` 형식으로 이름 정리를 제안하도록 개선했습니다.
- 메인 목록의 개별 파일을 더블클릭하면 탐색기에서 상위 폴더만 여는 대신 해당 파일이 선택된 상태로 열리도록 복구했습니다.
- 건강 진단 상세 내용과 복사 결과의 파일 목록에 각 파일의 용량을 함께 표시해 권·화 구성을 검토하기 쉽게 개선했습니다.

## v9.0.58

- 건강 진단 이름 변경 미리보기에서 권/화 단위 선택 칸을 넓혀 글자와 화살표가 답답하게 보이지 않도록 조정했습니다.
- 건강 진단에서 `(미완 종결)`의 `완`을 완결 표식으로 잘못 분리해 추천 파일명이 깨지던 문제를 수정하고 `(미완결)`로 정리하도록 보강했습니다.
- 건강 진단에서 `[1500px업스]`처럼 붙어 있는 해상도와 업스케일 표기를 `[1500px][업스]`처럼 별도 태그로 나눠 추천하도록 개선했습니다.
- 건강 진단에서 해상도와 연재본 태그를 재배치한 뒤 `_` 구분자만 추천명에 남던 문제를 수정했습니다.
- 건강 진단에서 이름 정리 항목을 여러 개 선택하면 각 파일의 현재 이름과 추천 이름을 `변경 전 → 변경 후` 형태로 함께 보여주도록 개선했습니다.
- 건강 진단의 확인됨/숨김 상태가 추천 문구나 이름 정리 규칙 변경만으로 풀리지 않도록 진단 항목 식별 방식을 안정화했습니다.
- 건강 진단 누락 검사에서 권·화 혼합 파일, 2부/시즌/Part/v버전 연속 번호, 페이지·버전 숫자를 실제 누락 권수로 잘못 판단하는 경우를 줄였습니다.
- 괄호 짝이 깨진 원본 파일명이나 자동 추천명은 이름 변경 대상에서 제외하고 직접 확인하도록 안전장치를 보강했습니다.
- 건강 진단 이름 정리에서 `1500px UPS` 같은 표기를 `[1500px][업스]`로 정리하고, 의미가 확실하지 않은 괄호 메모는 임의로 태그화하지 않도록 개선했습니다.
- 전체 스캔이 정상 완료되면 현재 스캔 범위에서 더 이상 사용하지 않는 오래된 스캔 캐시를 안전하게 정리하도록 개선했습니다.
- 건강 진단에서 `6.0`처럼 소수점이 포함된 번호를 `.0`으로 잘못 분리하지 않고, 권/화 단위를 바꿔도 `6.0권` 또는 `6.0화`로 온전히 유지하도록 수정했습니다.
- 건강 진단에서 `(정&단면)`처럼 함께 적힌 표기를 `[정][단면]`처럼 각각의 태그로 정리하도록 개선했습니다.
- 건강 진단의 이름 변경 미리보기에서 추천 파일명 셀을 더블클릭해 바로 편집하고, 기존 자동 추천으로 표현하기 어려운 이름도 안전 검사를 다시 거쳐 변경할 수 있도록 개선했습니다.
- 건강 진단에서 `(21권 완결)`처럼 권수와 완결 표기가 하나의 괄호 안에 적힌 파일을 `21권 (완결)` 형태로 올바르게 추천하도록 수정했습니다.
- 건강 진단에서 `[1-13권]`처럼 괄호로 감싼 권수·화수 범위를 `1-13권` 형태로 정리하고, `(580원본)`이나 `[1080연재]`처럼 붙어 있는 해상도·출처·판형 표기를 각각의 표준 태그로 분리하도록 개선했습니다.
- 건강 진단에서 추천 파일명을 편집하거나 실제 이름 변경·변경 취소를 실행한 뒤에도 목록과 미리보기의 기존 스크롤 위치가 유지되도록 개선했습니다.
- 건강 진단에서 `화석`, `화집`처럼 `화`로 시작하는 일반 단어의 첫 글자를 화수 단위로 오인해 추천 파일명에서 삭제하던 문제를 수정했습니다.
- 건강 진단에서 가장 최근에 성공한 이름 변경 묶음을 원래 이름으로 되돌릴 수 있도록 추가하고, 원래 이름 충돌이나 파일 소실이 있으면 해당 파일을 건드리지 않도록 보강했습니다.

## v9.0.53

- 대량 라이브러리의 건강 진단에서 같은 이름 정리 규칙과 폴더 파일 목록을 반복 계산하던 부분을 줄여 진단 시작 대기 시간을 단축했습니다.
- 스캔 완료 뒤 라이브러리 목록과 분석 캐시를 저장하는 동안 메인 화면이 멈추는 부담을 줄이고, 저장에 실패하면 변경 알림을 유지해 다시 시도할 수 있도록 보강했습니다.
- 최근 스캔 미반영 상세에서 최소 크기 제외, 중복 수집, 최종 미반영을 구분해 원인을 더 쉽게 확인할 수 있도록 개선했습니다.
- 라이브러리 목록과 분석 캐시 SQLite 파일의 잠금 대기와 무결성 확인을 보강했습니다.

## v9.0.52

- 스캔 폴더 넓게 보기 창의 하단 버튼을 `적용`, `취소` 순서로 정리했습니다.
- 스캔 폴더 넓게 보기와 개별 분석·통합 비교·목록 검사·건강 진단 같은 보조 창을 사용하는 동안 뒤의 메인 목록에서 Shift 표지 미리보기가 나타나지 않도록 수정했습니다.
- 연관 검색에서 `절대령역`처럼 장식 별표를 생략한 검색어로 `절대☆령역` 같은 제목도 찾을 수 있도록 보완했습니다.
- 파일명 끝에 괄호 없이 `AI번역`이 붙고 마지막 권에 완결 표기가 함께 있는 경우 시리즈가 분리되고 일부 권만 이름 정리 후보로 표시되던 문제를 수정했습니다.
