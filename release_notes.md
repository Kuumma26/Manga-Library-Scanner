## v8.2.5

- 목록 검사에서 시즌별로 나뉜 보유 권수와 입력 제목 매칭을 보강해 `환괴지대 – 1~2권` 같은 항목의 미보유 오탐을 줄임
- 목록 검사에서 `이토 준지 단편집1 BEST OF BEST`처럼 붙어 있는 제목도 더 자연스럽게 찾아 보유 항목으로 인식하도록 개선
- 자동 업데이트 확인에서 일부 PC 환경의 네트워크 차이로 업데이트 파일이 있어도 못 받는 문제를 줄이기 위해 다운로드 확인 방식을 보강
- 업데이트 창에 `수동 업데이트` 버튼을 추가하고 버튼 구성을 정리해, 자동 업데이트가 안 될 때도 바로 릴리즈 페이지로 이동 가능
- 자동 업데이트 진행 안내를 단계형 텍스트 창으로 다듬고, 콘솔 창은 숨기면서 안내창은 보이도록 정리
- 메인 검색에서 제목/파일명 검색 준비 캐시를 재사용하도록 정리해 첫 검색과 반복 검색 반응성을 개선
- 스캔 시작 후 파일 분석 단계로 넘어갈 때 진행 바가 다시 눈에 띄게 올라가도록 진행 표시 전환 타이밍을 보강
- 스캔 중 `분석 시작 / 분석 중...` 상태 문구와 진행 바가 실제로 먼저 그려진 뒤 다음 단계로 넘어가도록 화면 반영 타이밍을 보강
- 스캔 진행 표시를 이벤트마다 누적 갱신하는 방식에서 현재 누적치만 주기적으로 읽어오는 스냅샷 표시 방식으로 정리해, 속도 저하 없이 `분석 시작 / 분석 중...`이 보이도록 개선
- 스캔 스레드 `Auto` 값을 CPU 최대치 대신 보수적인 권장값으로 조정해, 고코어 PC에서도 과한 병렬도로 UI가 멈추는 현상을 줄임
- 스캔 관련 병렬 처리 전반이 환경설정 `스캔 스레드 수`를 공통 기준으로 따르도록 정리해, 설정값이 폴더 수집/파일 분석/통합 비교에 일관되게 반영되도록 개선

## v8.2.4

- 목록 검사에서 시즌 분리 작품과 일부 이토 준지 단편집 제목이 실제 보유 상태에 더 자연스럽게 매칭되도록 개선
- 목록 검사 입력의 특수 대시 표기(–)가 섞여도 보유 권수 판정이 더 안정적으로 동작하도록 보정
- 일부 네트워크 환경에서 자동 업데이트 준비 확인이 더 안정적으로 통과하도록 보강
- 업데이트 창 가로폭과 버튼 구성을 정리하고, 수동 업데이트 버튼을 추가해 접근성을 개선
- 자동 업데이트 진행 안내를 단순 텍스트형으로 정리하고 PowerShell 창 노출을 줄이도록 개선
- 자동 업데이트 진행창을 더 깔끔한 카드형 안내로 다듬고, 진행 단계가 바뀌는 모습이 보이도록 개선

## v8.2.3

- 목록 검사 결과창에서 우클릭 분석이 정상 동작하지 않던 문제를 수정 (v8.2.3)

## v8.2.2

- 앱 실행 시 새 버전이 있으면 업데이트 창이 자동으로 열리고, 원하면 24시간 동안 다시 알리지 않도록 설정할 수 있게 개선
- 목록 검사에서 비슷한 단어가 일부 겹치는 다른 작품으로 잘못 매칭되던 오탐을 줄여 정확도를 개선
- 검색과 목록 검사에서 제목 앞부분이 더 비슷한 작품이 우선되도록 매칭 우선순위를 조정
- 연관 검색에서 란마½ / 란마 1/2 같은 분수 표기도 란마 1 2 검색어와 자연스럽게 이어지도록 검색 정확도를 개선
- 메인 목록 우클릭 동작이 다중 선택을 더 자연스럽게 따르도록 개선
- 환경설정에 새로운 테마 5종(Matcha Mist, Graphite Mint, Aurora Plum, SynthWave, Tokyo Night Light)을 추가해 취향에 맞는 분위기로 선택할 수 있게 개선
- 메인 목록과 목록 검사 결과창에서 컬럼 경계선을 더블클릭하면 해당 열 내용 길이에 맞춰 폭이 자동으로 조정되도록 개선
- 목록 검사 결과창도 긴 셀 텍스트에 마우스를 올리면 잘린 내용을 툴팁으로 볼 수 있도록 통일

## v8.2.1

- 처음 실행하거나 저장된 라이브러리가 없을 때 상태바에 초기 사용 안내 문구가 명확하게 표시되도록 개선

## v8.2.0

- v8.2.0 마이너 업데이트 (이전 내부 구조, UI 최적화 반영 및 안정화)

## v8.1.9

- 자동 업데이트 중 기존 버전 종료 뒤 잠시 비는 구간에도 별도 진행 안내창이 더 안정적으로 보이도록 개선
- 스캔 시작 직후 파일 탐색 시작... 상태에서 진행이 멈추던 문제 수정
- 메인 화면 라이브러리 목록 아래 진행 표시 영역 간격을 더 타이트하게 정리

## v8.1.8

- 앱 사용 중 간헐적으로 스캔(검색) 목록이 눈앞에서 사라지던(초기화) 현상 수정
- 스캔 완료 후 분석에서 제외되었거나 누락/건너뛴 폴더가 있을 경우 상세 정보를 알려주는 알림창 추가
- 검색 결과 목록 표시 체감 속도 향상
- 도움말 설명 말풍선의 작동 안정성 개선

## v8.1.7

- 시작 시 나타나는 패치노트 알림창이 초기 환경 로딩 완료 후 안전하게 표시되도록 안정성 향상
- 기타 설정 및 패치노트 화면 레이아웃(UI) 관련 최적화

## v8.1.6

- 화면 분할 창 비율 유지 안정성 향상
- 신규 업데이트 확인 알림창의 표시 정확도 및 반응성 개선

## v8.1.5

- 검색 결과 목록 표시 속도 및 안정성 개선
- 중복 도서 및 누락 권수를 찾아내는 분석 처리 속도 향상
- 스캔 목록 텍스트 내보내기(저장) 기능의 반응 속도 개선

## v8.1.4

- 버전 동기화를 위한 앱 내부 릴리즈

## v8.1.3

- 새 업데이트 알림창 패치노트 추가 표시 UI 개선

## v8.1.2

- 수동 업데이트 확인 시 불필요하게 팝업되던 패치노트 알림창 예외처리 등 표시 조건 개선

## v8.1.1

- 애플리케이션 시스템 및 내부 안정성 개선

## v8.1.0

- 프로그램 내부에 전용 '패치노트' 창이 신설되어, 이번 업데이트뿐만 아니라 과거의 모든 변경 내역을 언제든 편하게 열람할 수 있습니다.
- 자동 업데이트를 시도할 때 백그라운드에서 다운로드 파일이 완전히 준비되었는지 먼저 확인하도록 하여 안정성을 더 높였습니다.

## v8.0.9

- 프로그램 자동 업데이트 과정 중 임시 작업 폴더가 남아있거나 올바르게 삭제되지 않던 문제를 고쳤습니다.
