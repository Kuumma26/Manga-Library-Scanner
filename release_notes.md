[안내]

- Everything처럼 모든 정보를 즉시 실시간으로 갱신하는 방식은 NAS 이용 환경에서 잦은 접근 부담이 생길 수 있어, 충분히 테스트하며 단계적으로 적용할 예정입니다. 현재는 파일 추가/수정/삭제가 감지되면 잠시 후 변경분만 갱신할 수 있도록 알려주고, 스캔 버튼 클릭 시 변경된 부분만 우선 스캔하도록 개선해 두었습니다.
- v9.0.0버전 부터는 저장 구조와 결과 목록 표시, 개별 분석, 통합 비교, 목록 검사 전반을 크게 최적화해 대량 라이브러리에서 시작 로딩과 목록 표시, 분석 후 대기 시간이 줄어들도록 개선했습니다. 기존 스캔 캐시와 라이브러리 목록은 새 SQLite 형식으로 자동 변환되며, 변환이 정상 완료되면 기존 JSON은 .bak으로 한 번 보관한 뒤 다음 실행에서 정리됩니다. 다만 캐시나 목록 변환이 반복 실패하는 경우에는 안전을 위해 빈 캐시/목록으로 시작할 수 있으므로, 이때는 폴더 스캔이나 필요한 분석을 다시 실행해 주세요.
- 라이브러리 건강 진단은 이제 누락 의심, 중복 의심, 이름 정리, 품질/출처 경고를 검토하는 단계에서 더 나아가 추천 파일명을 미리 확인하고 실제 이름 변경까지 진행할 수 있도록 확장되었습니다. 이름 변경 전에는 원본 파일 존재 여부, 같은 폴더 이름 충돌, 변경 계획 중복을 먼저 확인하고 안전한 항목만 기본 선택되며, 권/화 단위도 미리보기에서 개별 또는 일괄로 바꿀 수 있습니다. 다만 건강 진단은 라이브러리 정리 보조 도구이므로, 탐지 결과나 추천명이 이상한 경우 파일명/경로와 기대한 결과를 자세히 적어 피드백으로 보내 주세요. 정리 과정에서 불편한 점, 새 규칙 제안, 더 좋은 아이디어도 함께 보내 주시면 이후 개선에 적극 반영하겠습니다.

## v9.0.30

- 건강 진단 이름 정리에서 `완결 1200px`, `1600px/1080px`, `3300px~3400px`처럼 완결 표기 뒤에 붙은 해상도를 `[1200px]` 같은 태그로 정리해 추천하도록 보강했습니다.
- 건강 진단 이름 정리에서 `(7~9 합본 (완결))(AI번역)`처럼 중첩 괄호가 섞인 파일명의 추천명이 깨질 수 있던 문제를 수정했습니다.

## v9.0.29

- 건강 진단 이름 정리에서 `[완]`, `[완결]`, `[完]`, `(完)`, `권完`처럼 적힌 완결 표기도 `(완결)`로 정리해 추천하도록 보강했습니다.
- 숫자와 `+` 기호가 제목 일부인 파일명, 예를 들어 `48+1 1권 [스캔] [양면].zip`이 제목 없음으로 처리되어 스캔 미반영으로 남을 수 있던 문제를 수정했습니다.

## v9.0.28

- ZIP/CBZ 파일 내용이 바뀌었는데 파일 크기와 수정 시간이 같게 유지된 경우, 개별 분석에서 예전 내부 폴더 구조가 캐시로 남아 보일 수 있던 문제를 보강했습니다.

## v9.0.27

- 건강 진단 창과 이름 변경 미리보기의 버튼/필터 툴팁이 UI 배율과 테마 폰트 크기를 반영하도록 보강했습니다.

## v9.0.26

- 건강 진단에서 실제 이름 변경을 지원하게 된 현재 동작에 맞춰 창 제목과 메인 도움말 문구에서 `읽기 전용` 표현을 제거했습니다.
- 건강 진단과 이름 변경 미리보기 목록에서 창이나 컬럼이 좁아져 텍스트가 말줄임될 때 원문을 툴팁으로 확인할 수 있도록 보강했습니다.
- 피드백 전송 제한 시간이나 설정 확인 문제로 전송이 바로 가능하지 않은 경우에도 피드백 작성 창은 열리도록 수정하고, 제한 안내는 창 안에서 확인할 수 있게 했습니다.
- 메인 결과 목록에서 `Alt+더블클릭` 또는 `Alt+Enter`로 선택한 파일을 바로 열 수 있도록 추가하고, 환경설정의 화면 탭에서 압축 파일/PDF별 외부 프로그램 연결을 지정할 수 있게 했습니다.
- 기존 사용자 습관을 유지하도록 메인 목록과 목록 검사의 일반 우클릭은 개별 분석/분석으로 유지하고, 메뉴는 `Shift+우클릭` 또는 `Alt+우클릭`으로 열 수 있게 도움말과 조작법 표시를 갱신했습니다.
- 목록 검사와 건강 진단 하단 조작법 표시를 메인 목록처럼 `전체 조작법` 표시로 맞춰, 창마다 안내 방식이 다르게 보이지 않도록 정리했습니다.
- 목록 검사와 건강 진단의 `전체 조작법` 툴팁도 메인 목록처럼 줄별 강조 색이 들어간 형식으로 통일했습니다.
- 패치노트에 건강 진단 중요 안내가 최신 안내와 이전 안내로 겹쳐 보이지 않도록, 같은 건강 진단 안내 주제는 최신 안내만 표시하도록 보강했습니다.
