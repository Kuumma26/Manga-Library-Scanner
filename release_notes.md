## v8.6.1

- 프로그램의 전체적인 기본 UI를 새롭게 개편하여 디자인과 애니메이션 동작, 반응성을 크게 향상시켰습니다.
- 검색 및 목록 갱신 시 화면 스크롤 위치가 보존되며 화면 깜빡임이 대폭 줄어들었습니다.
- 테마(다크 모드 등) 및 폰트 설정 변경 시 화면에 즉시 적용되는 속도를 최적화했습니다.
- 목록 검사에서 하이픈 뒤 권수, `+-` 해상도 메모, 연령 표기, 상/하 완결 표기처럼 헷갈리기 쉬운 텍스트도 더 정확하게 제목과 권수로 분리해 인식하도록 개선했습니다.
- 일부 환경에서 자동 업데이트 후 프로그램이 정상적으로 다시 열리지 않을 수 있던 문제를 수정했습니다.

## v8.4.8

- 검색과 목록 갱신 반응 속도를 개선했습니다.
- 라이브러리 스캔 진행 표시와 시작 전후 반응성을 개선했습니다.
- 환경설정의 중복 제외 옵션이 어떤 키워드 설정을 참고하는지 더 알아보기 쉽게 안내를 보강했습니다.
- 몬스터/몬스터 완전판처럼 상위 폴더명에는 판본 수식어가 없고 파일명에만 수식어가 붙은 경우에도 보관용 하위폴더 중복 제외가 더 자연스럽게 적용되도록 개선했습니다.

## v8.4.7

- 검색과 목록 갱신 반응 속도를 개선
- 라이브러리 스캔 진행 표시와 시작 전후 반응성을 개선
- 통합 비교와 해상도 분석 창의 버튼/결과 표시 안정성을 개선
- 스캔 설정에 ZIP/CBZ 압축 손상 검사 옵션을 추가하고 손상 파일 안내를 복구

## v8.4.5

- 목록 검사에서 `/`로 이어 붙은 두 작품 입력도 각각 따로 분리해 판정하도록 보강해, 첫 작품만 부분 보유로 잘못 잡히던 오탐을 줄임
- 연관 검색에서 전혀 맞지 않는 파일 후보를 더 빨리 건너뛰도록 정리해, 긴 검색어 검색 속도를 개선

## v8.4.4

- 목록 검사 실행을 백그라운드 worker로 옮겨 입력 줄이 많아도 창이 덜 멈추고, 검사 중 진행 상황이 보이도록 개선
- 목록 검사 진행 표시는 과한 애니메이션과 숫자 갱신을 빼고, 정적인 `검사 중...` 상태 안내 중심으로 더 단순하게 정리
- 연관 검색도 먼저 안 맞는 시리즈를 걸러낸 뒤 점수 계산을 하도록 정리해 긴 검색어 검색 반응성을 보강
- 파일명 끝에 띄어쓴 `完` 태그가 있어도 마지막 권수를 정상 인식해 권수 요약이 빠지지 않도록 보강
