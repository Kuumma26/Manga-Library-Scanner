[안내]

- Everything처럼 모든 정보를 즉시 실시간으로 갱신하는 방식은 NAS 이용 환경에서 잦은 접근 부담이 생길 수 있어, 충분히 테스트하며 단계적으로 적용할 예정입니다. 현재는 파일 추가/수정/삭제가 감지되면 잠시 후 변경분만 갱신할 수 있도록 알려주고, 스캔 버튼 클릭 시 변경된 부분만 우선 스캔하도록 개선해 두었습니다.
- v9.0.0버전 부터는 저장 구조와 결과 목록 표시, 개별 분석, 통합 비교, 목록 검사 전반을 크게 최적화해 대량 라이브러리에서 시작 로딩과 목록 표시, 분석 후 대기 시간이 줄어들도록 개선했습니다. 기존 스캔 캐시와 라이브러리 목록은 새 SQLite 형식으로 자동 변환되며, 변환이 정상 완료되면 기존 JSON은 .bak으로 한 번 보관한 뒤 다음 실행에서 정리됩니다. 다만 캐시나 목록 변환이 반복 실패하는 경우에는 안전을 위해 빈 캐시/목록으로 시작할 수 있으므로, 이때는 폴더 스캔이나 필요한 분석을 다시 실행해 주세요.

## v9.0.18

- 스캔 경로 목록에 스플릿 버튼 형태의 프리셋 메뉴를 추가해, 현재 폴더 체크 상태를 이름으로 저장하고 나중에 빠르게 다시 적용할 수 있도록 했습니다.
- 프리셋 적용으로 새로 켠 경로가 아직 저장된 라이브러리 목록에 없을 때만 전체 스캔이 필요하다는 안내를 표시하도록 했습니다.
- 스캔 경로 체크 상태는 목록 표시 필터로 쓰고, 스캔/변경 감지는 등록된 경로 기준으로 진행되도록 정리해 체크를 모두 해제해도 라이브러리 캐시는 갱신할 수 있게 보강했습니다.
- 상위 스캔 경로와 하위 스캔 경로를 함께 등록했을 때, 하위 경로 체크를 해제하면 해당 하위 경로의 저장된 목록과 페이지 조회 결과도 함께 제외되도록 보강했습니다.
- 스캔 경로 체크 상태는 스캔 대상 제외가 아니라 목록 표시/필터 기준으로 동작하도록 정리해, 체크 해제한 경로도 라이브러리 캐시는 스캔으로 최신화되게 맞췄습니다.
- 스캔 경로 목록에서 `//서버/공유`처럼 슬래시가 섞인 UNC 경로도 Windows 백슬래시 형태로 일관되게 표시되도록 정리했습니다.
- 환경설정 키워드 탭에 검색 입력칸을 추가해, 키워드 항목이 많을 때 항목명/설명/내용으로 빠르게 필터링할 수 있도록 했습니다.

## v9.0.17

- 환경설정 키워드 탭에 작가명 구분 키워드를 추가해, 같은 제목이지만 작가가 다른 시리즈를 구분할 때 참고할 기본 키워드를 사용자가 조정할 수 있도록 했습니다.
- 환경설정 키워드 탭에서 각 키워드 항목을 개별 초기화하거나 키워드 탭 전체를 기본값으로 되돌릴 수 있도록 보강했습니다. 초기화 후에는 저장을 눌러야 실제 적용됩니다.
- 환경설정 키워드 탭의 편집/초기화 버튼을 세로로 정리하고, 환경설정 초기화 계열 버튼 아이콘을 `↺`로 통일해 구분하기 쉽게 했습니다.
- 제목 안에 들어간 숫자와 뒤쪽 권/화/편/회 표기를 더 안정적으로 구분해, `v02 04편`, `03회` 같은 파일명이 잘못 묶이거나 권수를 잘못 읽는 경우를 줄였습니다.
- 일부 권에만 작가명이 붙어 기본 제목과 작가명 제목으로 갈라진 경우, 단일 작가명 suffix라면 누락 검사에서 같은 시리즈로 보정해 오탐을 줄였습니다.

## v9.0.16

- 제목에 `No.9`처럼 숫자가 포함된 작품이 권수로 잘못 인식되어 중복/누락 후보로 표시될 수 있던 문제를 수정했습니다.
- `제08권+외전`처럼 권수 뒤에 외전 표기가 붙은 파일과 `04 완결`처럼 권 없이 완결 표기가 붙은 파일의 권수/제목 인식을 보강했습니다.
- 같은 제목을 다른 작가명으로 관리하는 `광인 01 []배광선` 같은 파일명이 서로 다른 시리즈로 구분될 수 있도록 보강했습니다.

## v9.0.15

- 목록 검사에서 `1-18 1500`, `1-10 완 1500.zip`처럼 권수 범위 뒤에 해상도 숫자가 붙은 입력을 부분 보유로 잘못 판정하던 문제를 수정했습니다.

## v9.0.14

- 피드백 전송 진행 상태 문구를 더 자연스럽게 정리했습니다.
